[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "fairvalue"
version = "0.1.0"
description = "S&P 500 (SPX) index vs E-mini (ES) futures fair value calculator"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
dependencies = []

[project.optional-dependencies]
# Live data providers use only the Python standard library (urllib), so no
# runtime dependencies are required.  pandas is optional, for analysis only.
analysis = ["pandas>=2.0"]
dev = ["pytest>=7"]

[tool.setuptools]
packages = ["fairvalue", "fairvalue.providers"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
